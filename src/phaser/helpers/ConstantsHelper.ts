// export function createPlayerOwner(playerIndex: number): string {
//     return `player${playerIndex}`;
// }

// export function createComputerOwner(computerIndex: number): string {
//     return `computer${computerIndex}`;
// }

// export const createCastleOwners = (playersCount: number, computersCount: number): string[] => {
//     const owners = [];

//     for (let i = 1; i <= playersCount; i++) {
//         owners.push(createPlayerOwner(i));
//     }

//     for (let i = 1; i <= computersCount; i++) {
//         owners.push(createComputerOwner(i));
//     }

//     return owners;
// };